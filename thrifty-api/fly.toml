# fly.toml - Thrifty Worker Configuration

app = "thrifty-worker"
primary_region = "fra"

[build]

[env]
NODE_ENV = "production"

[processes]
worker = "npm run worker:prod"

[[vm]]
memory = "256mb"
cpu_kind = "shared"
cpus = 1
